#pragma once
#include <string>
#include <vector>
#include <map>
#include <memory>
#include <iostream>


namespace RemoteWmi {
    using std::string;
    using std::wstring;
    using std::vector;
    using std::map;
    using std::pair;
    using std::make_pair;
    using std::unique_ptr;
    using std::make_unique;
}
